# 开发状态

版本：0.3.2 MVP

## v0.3.2 更新

- 报告收缩为：核心观点、审查结论、可能利好行业、可能受益公司、关键风险和最多3条依据。
- 删除新帖处理中的原子主张列表、明示/隐含前提、替代解释、逐条事实核查等冗余输出。
- 每帖模型调用由“翻译 + 分析 + N条事实逐项核查”降为固定2次，降低等待时间与 Token 消耗。
- 每帖最多2条网页检索，输出最多3个行业、5家公司和3条风险，不确定时不凑数。

## v0.3.1 更新

- 所有企业微信推送统一改为群机器人 Webhook：实时快讯、完整 Markdown 报告和每日汇总共用一个配置。
- 移除 CorpID、AgentID、应用 Secret、接收成员 UserID、access token、素材上传和 MPNews 链路。
- 保留实时推送与每日汇总独立开关，并兼容迁移 v0.3.0 的实时推送开关。
- 增加 Webhook 域名与路径校验、接口异常解析和机器人请求测试。

## v0.3.0 更新

- 实时推送由群机器人迁移至企业微信自建应用：Text 快讯 + MPNews 完整图文。
- 群机器人改为每日汇总专用，支持时区、发送时间和成功日期去重。
- 推送错误与分析管线解耦；推送失败不会丢失或中断本地事实核查报告。
- 设置页增加 CorpID、AgentID、应用 Secret、接收成员 UserID、通道开关和独立测试按钮。
- GitHub Actions 更新为 `macos-15-intel` 与 `macos-15`，替换已退役的 `macos-13`。

## v0.2.0 更新

- 增加 X API v2 App-only Bearer Token 官方采集适配器。
- 只要保存了 Bearer Token 就自动优先使用官方渠道；官方凭据失效时明确报错，不静默回退到 `twscrape`。
- 增加官方接口测试、用户 ID 缓存、帖子/引用/媒体映射、限流错误提示和 Token 安全存储。
- 设置页显示当前优先渠道，并支持显式清除官方 Token 后恢复 Cookie 渠道。

## 已完成

- Windows/macOS Electron 桌面壳、托盘和登录自启动。
- 设置、概览、账号、帖子报告、运行日志五个页面。
- 操作系统安全存储、普通数据原子写入和本地历史限制。
- Python `twscrape` JSON Lines sidecar 与临时会话库。
- 账号基线、过滤、轮询抖动、指数退避和 X 帖子 ID 去重。
- OpenAI Responses API 翻译、主张拆解、逻辑分析和证据判断。
- DuckDuckGo 免费检索 Provider 与来源初步分级。
- 企业微信机器人实时快讯、完整报告、每日汇总、UTF-8 字节限制、去重和重试。
- PyInstaller + electron-builder 打包链路和三平台架构 CI。

## 发布前必须完成

1. 使用真实专用 X 账号验证 Cookie 登录与 10 个目标账号连续采集。
2. 使用真实模型 API 和企业微信群机器人 Webhook 完成端到端测试。
3. 在 Windows x64、macOS Intel、macOS Apple Silicon 实机验证安装、托盘、开机启动和休眠恢复。
4. 配置 Windows 代码签名、Apple Developer ID 签名与公证。
5. 运行 72 小时稳定性测试，并按人工标注的 100 条帖子评测分析质量。

这些步骤依赖外部凭据、目标账号和对应操作系统，未在无凭据的开发环境中伪造通过。
